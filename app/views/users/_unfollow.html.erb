<%= form_for(current_user.active_relationships.find_by(followed_id: @user.id),
		html: { method: :delete }, remote: true) do |f| %> <!--remote: true automagically gets Rails to use Ajax. Using a simple HTML property instead of inserting the full JS code, Rails follows philosophy of 'unobtrusive JS'.-->
	<%= f.submit "Unfollow", class: "btn" %>
<% end %>